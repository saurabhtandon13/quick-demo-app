apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: shoppingapp-route
  namespace: webapps
spec:
  parentRefs:
  - name: cloudapps-gateway
  hostnames:
  - shoppingapp.cloudapps.lvnlab.internal
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: frontend-external.webapps.svc.cluster.local
      port: 80
